# Our-Group-Project
The goal of this task is to create an interactive calculator that can: - Perform addition, subtraction, multiplication, division - Compute power and square root - Exit when the user chooses  We will implement the calculator using functions.   The program will keep running in a loop until the user selects exit
